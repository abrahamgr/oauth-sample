{
  "name": "oauth-sample",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "biome check --write",
    "clean:build": "rm -rf ./turbo ./packages/*/.turbo ./packages/*/dist ./packages/*/build",
    "dev": "turbo run dev",
    "format": "biome format --write .",
    "lint": "biome check .",
    "prepare": "lefthook install && ./link-skills.sh || true"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "lefthook": "^2.1.5",
    "turbo": "^2.9.6",
    "typescript": "^6.0.2"
  },
  "engines": {
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.1.2"
}
